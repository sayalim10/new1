<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <!-- Draw a 5dp width border around shape -->
            <solid android:color="@color/toggle_btn_line_off"/>
            <corners android:radius="4dp" />
        </shape>
    </item>
    <!-- Overlap the left, top and right border using background color  -->
    <item android:bottom="5dp">
        <shape android:shape="rectangle">
            <padding android:bottom="0dp" android:top="0dp" android:left="2dp" android:right="2dp"/>
            <solid android:color="@color/button_unselector" />
        </shape>
    </item>
</layer-list>